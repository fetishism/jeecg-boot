spring:
  application:
    name: jeecg-gateway
  profiles:
    active: #spring.profiles.active#
  cloud:
    config:
      # 是否启用配置中心
      enabled: ${SPRING_CLOUD_CONFIG_ENABLED:false}
      import:
        - optional:nacos:${spring.application.name}-@profile.name@.yaml
      # 配置中心地址
      uri: ${SPRING_CLOUD_CONFIG_URI:http://dev.hzero.org:8010}
      fail-fast: false
      retry:
        # 最大重试次数
        maxAttempts: 6
        multiplier: 1.1
        # 重试间隔时间
        maxInterval: 2000
      # 标签
      label: ${SPRING_CLOUD_CONFIG_LABEL:}
    nacos:
      username: ${NACOS_SERVER_USERNAME:nacos}
      password: ${NACOS_SERVER_PASSWORD:nacos}
      config:
        enabled: true
        namespace: ${NACOS_CONFIG_NAMESPACE:78755410-f295-494c-ab33-338241351590}
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}
        file-extension: yml
        refresh-enabled: true
        #加载额外配置，除加载上面主配置文件外，额外加载的公有配置功能
        extension-configs:
          - data-id: application.yml
            refresh: true
          - data-id: application-dev.yml
            refresh: true
